const name="hello"
console.log("hello"+name);  //This is old one
console.log(`hello ${name}`); //Latest method->mostly used in backend 
console.log(name[0]);
//.toString() -> function to convert number to string
//.toFixed(1)->set precision as 1